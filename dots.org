* Emacs
** Custom configuration
*** Convert tabs to white spaces
      #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
        (setq-default indent-tabs-mode nil)
      #+end_src

*** Make tabs to be 2 whitespaces width
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (setq-default tab-width 2)
    #+end_src

*** Does anyone type yes anymore?
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (fset 'yes-or-no-p 'y-or-n-p)
    #+end_src

*** Do not display buffers with the name ~*Async Shell Command*~
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (add-to-list 'display-buffer-alist
                   (cons "\\*Async Shell Command\\*.*" (cons #'display-buffer-no-window nil)))
    #+end_src

*** This plays nicely with lsp and company
      #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
        (setq read-process-output-max (* 1024 1024))
        (setq gc-cons-threshold 100000000)
      #+end_src

** GUI
*** Hiding elements
    This hide several elements like menu, too bar, etc.
    in GUI mode
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (when window-system
        (menu-bar-mode -1)
        (tool-bar-mode -1)
        (scroll-bar-mode -1)
        (tooltip-mode -1))
    #+end_src

*** Start in a blank page
    This allow to start emacs in a blank page
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (setq inhibit-startup-message t)
      (setq initial-scratch-message "")
    #+end_src
    
*** Stop blinking cursor
    Turn off the blinking cursor
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (blink-cursor-mode -1)
    #+end_src

*** Show-paren-mode
    Highlight the the matching (){}[]
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (show-paren-mode t)
    #+end_src

*** Highlight current line
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (global-hl-line-mode t)
      (add-hook 'prog-mode-hook #'hl-line-mode)
      (add-hook 'org-mode-hook #'hl-line-mode)
    #+end_src

** Packages
*** straight.el
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (defvar bootstrap-version)

      (let ((bootstrap-file
             (expand-file-name "straight/repos/straight.el/bootstrap.el" user-emacs-directory))
            (bootstrap-version 5))
        (unless (file-exists-p bootstrap-file)
          (with-current-buffer
              (url-retrieve-synchronously
               "https://raw.githubusercontent.com/raxod502/straight.el/develop/install.el"
               'silent 'inhibit-cookies)
            (goto-char (point-max))
            (eval-print-last-sexp)))
        (load bootstrap-file nil 'nomessage))

      (setq straight-use-package-by-default t)
    #+end_src

*** use-package
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (straight-use-package 'use-package)
    #+end_src
    
*** org-mode
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package aldama/org-config
        :straight nil
        :no-require t
        :custom
        (org-startup-with-inline-images t)
        ;; inline latex like $y=mx+c$ will appear in a different colour in an org-mode file to help it stand out
        (org-highlight-latex-and-related '(latex))
        (org-ellipsis "…")
        ;; syntax highlight
        (org-src-fontify-natively t)
        (org-src-tab-acts-natively t)
        (org-src-window-setup 'current-window)
        :config
        (add-hook 'org-mode-hook (lambda ()
                                   "Beautify Org Checkbox Symbol"
                                   (push '("[ ]" .  "☐") prettify-symbols-alist)
                                   (push '("[X]" . "☑" ) prettify-symbols-alist)
                                   (push '("[-]" . "❍" ) prettify-symbols-alist)
                                   (push '("#+BEGIN_SRC" . "↦" ) prettify-symbols-alist)
                                   (push '("#+END_SRC" . "⇤" ) prettify-symbols-alist)
                                   (push '("#+BEGIN_EXAMPLE" . "↦" ) prettify-symbols-alist)
                                   (push '("#+END_EXAMPLE" . "⇤" ) prettify-symbols-alist)
                                   (push '("#+BEGIN_QUOTE" . "↦" ) prettify-symbols-alist)
                                   (push '("#+END_QUOTE" . "⇤" ) prettify-symbols-alist)
                                   (push '("#+begin_quote" . "↦" ) prettify-symbols-alist)
                                   (push '("#+end_quote" . "⇤" ) prettify-symbols-alist)
                                   (push '("#+begin_example" . "↦" ) prettify-symbols-alist)
                                   (push '("#+end_example" . "⇤" ) prettify-symbols-alist)
                                   (push '("#+begin_src" . "↦" ) prettify-symbols-alist)
                                   (push '("#+end_src" . "⇤" ) prettify-symbols-alist)
                                   (prettify-symbols-mode)))
        )
    #+end_src

*** evil
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package evil
        :custom
        ;; change the color of the cursor
        (evil-normal-state-cursor '("gray" box))
        (evil-visual-state-cursor '("orange" box))
        (evil-insert-state-cursor '("dodger blue" bar))
        (evil-replace-state-cursor '("red" bar))
        (evil-disable-insert-state-bindings t)
        (evil-want-keybinding nil)
        :config
        (evil-set-initial-state 'wdired-mode 'normal)
        (evil-set-initial-state 'ebib-index-mode 'emacs)
        (evil-set-initial-state 'eshell-mode 'normal)
        (evil-set-initial-state 'pdf-view-mode 'normal)
        (evil-set-initial-state 'pdf-outline-buffer-mode 'normal)
        (evil-mode 1))
    #+end_src

**** evil-escape
     #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
       (use-package evil-escape
       :after (evil)
       :custom (evil-escape-key-sequence "jk")
       :config (evil-escape-mode))
     #+end_src

**** evil-commentary
     #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
       (use-package evil-commentary)
     #+end_src

**** evil-surround
     #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
       (use-package evil-surround
         :config
         (global-evil-surround-mode 1))
     #+end_src

*** general.el
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package general
        :config
        (general-evil-setup))
    #+end_src

*** doom themes
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package doom-themes
        :custom
        (doom-themes-enable-bold t)    ; if nil, bold is universally disabled
        (doom-themes-enable-italic t)  ; if nil, italics is universally disabled
        (doom-themes-treemacs-theme "doom-colors") ; use the colorful treemacs theme
        :config
        ;; Load the theme (doom-one, doom-molokai, etc); keep in mind that each theme
        ;; may have their own settings.
        (load-theme 'doom-one t)
        ;; (load-theme 'doom-solarized-dark t)

        ;; Enable flashing mode-line on errors
        (doom-themes-visual-bell-config)

        ;; or for treemacs users
        (doom-themes-treemacs-config)

        ;; Corrects (and improves) org-mode's native fontification.
        (doom-themes-org-config))
    #+end_src
    
*** dired
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package dired
        :straight nil
        :hook (dired-mode . dired-hide-details-mode)
        :custom
        (dired-listing-switches "-Bhl --group-directories-first -v")
        (dired-dwim-target t) ;;use to copy to the next buffer visible
        :config
        (set-face-attribute 'dired-header nil
                            :foreground "#282c34"
                            :weight 'bold)

        (eval-after-load "dired-aux"
          '(add-to-list 'dired-compress-file-suffixes
                        '("\\.zip\\'" ".zip" "unzip")))

        (defvar dired-compress-files-alist
          '(("\\.tar\\.gz\\'" . "tar -c %i | gzip -c9 > %o")
            ("\\.zip\\'" . "zip %o -r --filesync %i"))
          "Control the compression shell command for `dired-do-compress-to'.

            Each element is (REGEXP . CMD), where REGEXP is the name of the
            archive to which you want to compress, and CMD the the
            corresponding command.

            Within CMD, %i denotes the input file(s), and %o denotes the
            output file. %i path(s) are relative, while %o is absolute.")

        (defcustom list-of-dired-switches
          '(("-Bhl --group-directories-first -v" . "directories first")
            ("-BhlA --group-directories-first -v" . "no . & .."))
          "List of ls switches (together with a name to display in the mode-line) for dired to cycle among.")

        (defun cycle-dired-switches ()
          "Cycle through the list `list-of-dired-switches' of switches for ls"
          (interactive)
          (setq list-of-dired-switches
                (append (cdr list-of-dired-switches)
                        (list (car list-of-dired-switches))))
          (dired-sort-other (caar list-of-dired-switches))
          (setq mode-name (concat "Dired " (cdar list-of-dired-switches)))
          (force-mode-line-update)))


      (use-package dired-x
        :straight nil
        :after dired
        :config
        (setq dired-guess-shell-alist-user '(("\\.zip$" "file-roller ? &>/dev/null")
                                             ("\\.tar\\.gz$" "file-roller ? &>/dev/null")
                                             ("\\.pdf$" "okular ? &>/dev/null")
                                             ("\\.mp4$" "vlc ? &>/dev/null")
                                             ("\\.torrent$" "transmission-gtk ? &>/dev/null")
                                             ("\\.epub$" "ebook-viewer ? &>/dev/null")
                                             ("\\.rar$" "file-roller ? &>/dev/null"))))

    #+end_src

*** counsel, ivy and swiper
**** ivy
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package ivy
        :custom
        (ivy-use-virtual-buffers t)
        (ivy-count-format "(%d/%d) ")
        :config
        (ivy-mode 1))
    #+end_src

**** counsel
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package counsel
        :commands (counsel-find-file councel-bookmark)
        :custom
        (counsel-find-file-ignore-regexp
         (concat
          ;; File names beginning with # or .
          "\\(?:\\`[#.]\\)"
          ;; File names ending with # or ~
          "\\|\\(?:\\`.+?[#~]\\'\\)"))

        (all-the-icons-ivy-file-commands
         '(counsel-find-file counsel-file-jump counsel-recentf counsel-projectile-find-file counsel-projectile-find-dir)))
    #+end_src

**** swiper
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
     (use-package swiper)
    #+end_src
    
*** doom-modeline
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package doom-modeline
        :hook (after-init . doom-modeline-mode)
        :custom
        ;; Whether display buffer encoding.
        (doom-modeline-buffer-encoding nil)
        (doom-modeline-icon nil))
    #+end_src

*** yasnippet
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package yasnippet
        :commands yas-minor-mode
        :custom
        (yas-snippet-dirs
         '("~/Projects/dots/snips/yasnippet"))
        :hook
        ((go-mode . yas-minor-mode)
         (org-mode . yas-minor-mode)
         (js-mode . yas-minor-mode)
         (dart-mode . yas-minor-mode)
         (python-mode . yas-minor-mode))
        :config
        (yas-reload-all))
    #+end_src

*** magit
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package magit)
    #+end_src

*** evil-magit
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package evil-magit
        :after (magit))
    #+end_src

*** lsp-mode
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package lsp-mode
        :straight (lsp-mode :type git :host github :repo "emacs-lsp/lsp-mode")
        :commands (lsp lsp-deferred)
        :hook
        ((go-mode . lsp-deferred)
         (python-mode . lsp-deferred)
         (dart-mode . lsp-deferred)
         (js-mode . lsp-deferred)
         (before-save . lsp-format-buffer)
         (before-save . lsp-organize-imports))
        :custom
        (lsp-dart-sdk-dir "~/bin/flutter/bin/cache/dart-sdk/")
        (lsp-signature-render-documentation nil)
        (lsp-gopls-complete-unimported t)
        :config
        (add-hook 'js-mode-hook (lambda () (setq js-indent-level 2)))
        (add-hook 'js-mode-hook (lambda () (setq tab-width 2)))
        )
    #+end_src

*** lsp-ui
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package lsp-ui
        :hook (lsp-mode . lsp-ui-mode)
        :custom
        ;; (lsp-ui-doc-header t)
        ;; (lsp-ui-doc-include-signature nil)
        (lsp-ui-doc-position 'at-point) ;; top, bottom, or at-point
        ;; (lsp-ui-doc-max-width 120)
        ;; (lsp-ui-doc-max-height 30)
        ;; (lsp-ui-doc-use-childframe t)
        ;; (lsp-ui-doc-use-webkit t)
        (lsp-ui-doc-enable nil)
        (lsp-ui-sideline-enable nil)
        (lsp-ui-sideline-show-code-actions nil)
        (lsp-ui-sideline-show-symbol t)
        (lsp-ui-sideline-show-hover t)
        (lsp-ui-sideline-show-diagnostics nil)
        ;; lsp-ui-imenu
        (lsp-ui-imenu-enable t)
        (lsp-ui-imenu-kind-position 'top))
    #+end_src

*** eglot
    #+begin_src emacs-lisp
      (use-package eglot
        :config
        (add-hook 'dart-mode-hook 'eglot-ensure))
    #+end_src

*** go-mode
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package go-mode)
    #+end_src

*** json-mode
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package json-mode)
    #+end_src

*** dart-mode
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package dart-mode
        :config
        (with-eval-after-load "projectile"
          (add-to-list 'projectile-project-root-files-bottom-up "pubspec.yaml")
          (add-to-list 'projectile-project-root-files-bottom-up "BUILD")))
    #+end_src

*** dart-server
    #+begin_src emacs-lisp 
      (use-package dart-server
        :custom
        (dart-server-sdk-path "~/bin/dart-sdk/bin/")
        (dart-server-enable-analysis-server t)
        (dart-format-on-save t)
        :config
        (add-hook 'dart-server-hook 'dart-mode)
        (add-hook 'dart-server-hook 'flycheck-mode))
    #+end_src

*** flutter
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package flutter
        :after dart-mode
        :custom
        (flutter-sdk-path "~/bin/flutter/")
        :bind
        (:map dart-mode-map
              ("C-M-x" . #'flutter-run-or-hot-reload)))
    #+end_src

*** company
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package company
        :hook
        (after-init . global-company-mode))
      ;; :config
      ;; Use tab key to cycle through suggestions.
      ;; ('tng' means 'tab and go')
      ;;(company-tng-configure-default)
    #+end_src

*** company-lsp
    #+begin_src emacs-lisp 
      (use-package company-lsp
        :config
        (push 'company-lsp company-backends))
    #+end_src

*** flycheck
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package flycheck
        :hook
        ((go-mode . flycheck-mode)
         (python-mode . flycheck-mode)
         (dart-mode . flycheck-mode)
         (js-mode . flycheck-mode))
        :custom
        (lsp-prefer-flymake nil))
    #+end_src

*** treemacs
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package treemacs)
    #+end_src

*** treemacs-icons-dired
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package treemacs-icons-dired
        :after treemacs dired
        :config )
    #+end_src

*** treemacs-evil
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package treemacs-evil
        :after treemacs)
    #+end_src

*** aggressive-indent
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package aggressive-indent
        :hook
        ((web-mode . aggressive-indent-mode)
         (json-mode . aggressive-indent-mode)
         (emacs-lisp-mode . aggressive-indent-mode)))
    #+end_src

*** projectile
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package projectile
        :init
        (projectile-mode +1))
    #+end_src

*** counsel-projectile
    #+BEGIN_SRC emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package counsel-projectile)
    #+END_SRC

*** avy
    Search for character
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package avy)
    #+end_src

*** smartparens
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package smartparens
        :config
        (require 'smartparens-config)

        (with-eval-after-load 'smartparens
          (sp-with-modes
              '(c++-mode dart-mode go-mode js-mode)
            (sp-local-pair "{" nil :post-handlers '(:add ("||\n[i]" "RET")))))

        (smartparens-global-mode t))
    #+end_src

*** org drill
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package org-drill)
    #+end_src

** Bindings
*** Normal-mode bindings
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package aldama/normal-mode-bindings
        :straight nil
        :after general
        :no-require t
        :general
        (:states 'normal
                 "f" 'avy-goto-char-2
                 "C-s" 'swiper
                 ;; swap ; and :
                 ";" 'evil-ex
                 ":" 'evil-repeat-find-char)
        (:keymaps 'company-active-map
                  "C-j" 'company-select-next
                  "C-k" 'company-select-previous))
    #+end_src

*** =,= Normal-mode bindings
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package aldama/normal-mode-bindings
        :straight nil
        :after general
        :no-require t
        :general
        (:states 'normal
                 :prefix ","
                 "ed" '(lambda() (interactive) (find-file "~/Projects/dots/dots.org"))
                 "en" '(lambda() (interactive)(find-file "~/Documents/notes/notes.org"))
                 "eb" '(lambda() (interactive)(find-file "~/Documents/notes/bookmarks.org"))
                 "d" '(lambda() (interactive) (dired default-directory))
                 "pp" 'counsel-projectile-switch-project
                 "pb" 'counsel-projectile-switch-to-buffer
                 "pf" 'counsel-projectile-find-file
                 "pg" 'counsel-projectile-grep
                 "b" 'counsel-switch-buffer
                 "s" 'magit-status
                 "q" 'mode-line-other-buffer))
    #+end_src

*** Dired bindings
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package aldama/dired-mode-bindings
        :straight nil
        :after general
        :no-require t
        :general
        (:states 'normal
                 :keymaps 'dired-mode-map
                 "j" 'dired-next-line
                 "k" 'dired-previous-line
                 "l" 'dired-find-file
                 "h" 'dired-up-directory
                 "yy" 'dired-do-copy
                 "yn" 'dired-copy-filename-as-kill
                 "yp" '(lambda() (interactive) (dired-copy-filename-as-kill 0))
                 "gk" '(lambda() (interactive) (dired "~/Documents"))
                 "gn" '(lambda() (interactive) (dired "~/Documents/notes"))
                 "gd" '(lambda() (interactive) (dired "~/Downloads"))
                 "gp" '(lambda() (interactive) (dired "~/Projects"))
                 "ge" '(lambda() (interactive) (dired "~/.emacs.d"))
                 "gc" '(lambda() (interactive) (dired "~/.config"))
                 "gy" '(lambda() (interactive) (dired "~/Projects/playground"))
                 "gb" '(lambda() (interactive) (dired "~/bin"))
                 "gm" '(lambda() (interactive) (dired "/media"))
                 "gh" '(lambda() (interactive) (dired "~"))
                 "m" 'dired-mark
                 "u" 'dired-unmark
                 "cw" 'dired-do-rename
                 "r" 'revert-buffer
                 "t" 'dired-hide-details-mode
                 "nd" 'dired-create-directory
                 "nf" 'counsel-find-file
                 "s" 'dired-do-async-shell-command
                 "q" 'quit-window
                 "w" 'dired-toggle-read-only
                 "W" 'wdired-finish-edit
                 "x" 'dired-do-compress
                 "za" 'cycle-dired-switches
                 "zd" 'dired-hide-details-mode
                 "d" 'dired-do-delete))
    #+end_src

*** Org bindings
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package aldama/org-bindings
        :straight nil
        :after general
        :no-require t
        :general
        (:states '(normal insert emacs)
                 :keymaps 'org-mode-map
                 "M-l" 'org-metaright
                 "M-h" 'org-metaleft)
        (:prefix ","
                 :states 'normal
                 :keymaps 'org-mode-map
                 "h" 'helm-org-in-buffer-headings
                 "n" 'org-toggle-narrow-to-subtree))
    #+end_src

*** Ivy bindings
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package aldama/ivy-mode-binding
        :straight nil
        :after general
        :no-require t
        :general
        (:keymaps 'ivy-minibuffer-map
                  "C-j" 'ivy-next-line
                  "C-u" 'counsel-up-directory
                  "C-k" 'ivy-previous-line))
    #+end_src

*** Programming bindings
    #+begin_src emacs-lisp :tangle ~/.emacs.d/init.el
      (use-package aldama/prog-mode-bindings
        :straight nil
        :after general
        :no-require t
        :general
        (:states 'normal
                 :keymaps 'prog-mode-map
                 "TAB" 'company-indent-or-complete-common
                 "<f6>" 'async-shell-command
                 "<f9>" 'lsp-ui-imenu
                 "gd" 'lsp-find-definition
                 "gp" 'lsp-ui-peek-find-references
                 "ge" 'treemacs
                 "K" 'lsp-ui-doc-mode
                 "gc" 'evil-commentary
                 "gb" 'evil-jump-backward)
        (:keymaps 'python-mode-map
                  "<f5>" '(lambda() (interactive) (async-shell-command (concat "python " (buffer-file-name)) "*python output*")))
        (:keymaps 'dart-mode-map
                  "<f5>" '(lambda() (interactive) (async-shell-command (concat "dart " (buffer-file-name)) "*dart output*")))
        (:keymaps 'js-mode-map
                  "<f5>" '(lambda() (interactive) (async-shell-command (concat "node " (buffer-file-name)) "*javascript output*")))
        (:keymaps 'go-mode-map
                  "<f5>" '(lambda() (interactive) (async-shell-command (concat "go run " (buffer-file-name)) "*go output*")))
        (:keymaps 'lsp-ui-peek-mode-map
                  "j" 'lsp-ui-peek--select-next
                  "k" 'lsp-ui-peek--select-prev))
    #+end_src

* I3wm
** Config
*** mod
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
   set $mod Mod4
   #+END_SRC
*** font
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
       font pango:Knack Nerd Font Regular 10
   #+END_SRC
*** windows
**** kill focused window
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
   bindsym $mod+q kill
   #+END_SRC
**** drag floating windows
    Use Mouse+$mod
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
   floating_modifier $mod
   #+END_SRC
**** change focus
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
    bindsym $mod+h focus left
    bindsym $mod+j focus down
    bindsym $mod+k focus up
    bindsym $mod+Tab focus right

    # alternatively, you can use the cursor keys:
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right
   #+END_SRC
**** move focused window
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
   bindsym $mod+Shift+Left move left
   bindsym $mod+Shift+Down move down
   bindsym $mod+Shift+Up move up
   bindsym $mod+Shift+Right move right
   #+END_SRC
**** uncategorized
   #+BEGIN_SRC sh :tangle ~/.config/i3/config

# split in horizontal orientation
bindsym $mod+Shift+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child
   #+END_SRC
*** Launching Programs
**** Tilix
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
   bindsym $mod+Return exec tilix -p Solarized dark
   #+END_SRC
**** Rofi (a program launcher)
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
   bindsym $mod+d exec "~/bin/scripts/rofi_bangs.dart"
   #+END_SRC
**** Emacs
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
     bindsym $mod+m exec "~/bin/emacs/src/emacs"
   #+END_SRC
**** Firefox
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
   bindsym $mod+Shift+f exec /home/last/bin/firefox/firefox --private-window
   #+END_SRC
*** workingspaces
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
     set $workspace1 "1"
     set $workspace2 "2"
     set $workspace3 "3"
     set $workspace4 "4"
     set $workspace5 "5"
     set $workspace6 "6"
     set $workspace7 "7"
     set $workspace8 "8"
     set $workspace9 "9"
     set $workspace0 "10"

     # move focused container to workspace
     bindsym $mod+Shift+1 move container to workspace $workspace1
     bindsym $mod+Shift+2 move container to workspace $workspace2
     bindsym $mod+Shift+3 move container to workspace $workspace3
     bindsym $mod+Shift+4 move container to workspace $workspace4
     bindsym $mod+Shift+5 move container to workspace $workspace5
     bindsym $mod+Shift+6 move container to workspace $workspace6
     bindsym $mod+Shift+7 move container to workspace $workspace7
     bindsym $mod+Shift+8 move container to workspace $workspace8
     bindsym $mod+Shift+9 move container to workspace $workspace9
     bindsym $mod+Shift+0 move container to workspace $workspace0

     # switch to workspace
     bindsym $mod+1 workspace $workspace1
     bindsym $mod+2 workspace $workspace2
     bindsym $mod+3 workspace $workspace3
     bindsym $mod+4 workspace $workspace4
     bindsym $mod+5 workspace $workspace5
     bindsym $mod+6 workspace $workspace6
     bindsym $mod+7 workspace $workspace7
     bindsym $mod+8 workspace $workspace8
     bindsym $mod+9 workspace $workspace9
     bindsym $mod+0 workspace $workspace0
   #+END_SRC

*** GUI
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
   # thin borders
   hide_edge_borders both

   set $bg-color 	         #2f343f
   set $inactive-bg-color   #2f343f
   set $text-color          #f3f4f5
   set $inactive-text-color #676E7D
   set $urgent-bg-color     #E53935

   # window colors
   #                       border              background         text                 indicator
   client.focused          $bg-color           $bg-color          $text-color          #00ff00
   client.unfocused        $inactive-bg-color $inactive-bg-color $inactive-text-color #00ff00
   client.focused_inactive $inactive-bg-color $inactive-bg-color $inactive-text-color #00ff00
   client.urgent           $urgent-bg-color    $urgent-bg-color   $text-color          #00ff00

   # Start i3bar to display a workspace bar (plus the system information i3status
   # finds out, if available)
   bar {
   # status_command i3status
   status_command i3blocks -c ~/.config/i3/i3blocks.conf
   mode hide
   hidden_state hide
   modifier Mod4
   colors {
   background $bg-color
   separator #757575
   #                  border             background         text
   focused_workspace  $bg-color          $bg-color          $text-color
   inactive_workspace $inactive-bg-color $inactive-bg-color $inactive-text-color
   urgent_workspace   $urgent-bg-color   $urgent-bg-color   $text-color
   }
   }

   for_window [class=".*"] border pixel 0
   #+END_SRC
*** Lock screen
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
   bindsym $mod+shift+x exec i3lock
   #+END_SRC
*** Restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
        bindsym $mod+Shift+r restart
   #+END_SRC
*** Exit i3 (logs you out of your X session)
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
        bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
   #+END_SRC
*** Print image
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
        # bindsym Print exec --no-startup-id scrot
        # bindsym Print exec screenshot
        # bindsym Shift+Print exec --no-startup-id scrot -u
        bindsym Print exec --no-startup-id scrot -u -e 'mv $f ~/Pictures/'
   #+END_SRC
*** Change keyboard language
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
   #change keyboard layout
   set $keyboardMode "keyboard d=de, e=us"
   bindsym $mod+Shift+i mode $keyboardMode
   mode $keyboardMode {
   bindsym d exec "setxkbmap de", mode "default"
   bindsym e exec "setxkbmap us", mode "default"
   # back to normal: Enter or Escape
   bindsym Return mode "default"
   bindsym Escape mode "default"
   }
   #+END_SRC
*** Music player mode
   #+BEGIN_SRC sh
   set $mode_music Music Player Mode
   bindsym $mod+m mode "$mode_music"

   mode "$mode_music" {
    bindsym a exec cmus-remote --prev
    bindsym c exec cmus-remote --pause
    bindsym s exec cmus-remote --next
    bindsym l exec cmus-remote --seek +5
    bindsym h exec cmus-remote --seek -5
    bindsym Up exec cmus-remote --vol +10%
    bindsym Down exec cmus-remote --vol -10%

    bindsym Escape mode "default"
   }

    bindsym $mod+Pause exec cmus-remote --pause
    bindsym $mod+Prior exec cmus-remote --seek -5
    bindsym $mod+Next exec cmus-remote --seek +5
   #+END_SRC
*** Audio control mode
   #+BEGIN_SRC sh :tangle ~/.config/i3/config
     bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
     bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
     bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound
   #+END_SRC
** i3blocks
   #+BEGIN_SRC sh :tangle ~/.config/i3/i3blocks.conf
   # The top properties below are applied to every block, but can be overridden.
   # Each block command defaults to the script name to avoid boilerplate.
   command=/usr/share/i3blocks/$BLOCK_NAME
   separator_block_width=15
   markup=none
   #+END_SRC
*** Volume indicator
   #+BEGIN_SRC sh :tangle ~/.config/i3/i3blocks.conf
   [volume]
   label=
   instance=Master
   interval=1
   signal=10
   command=/usr/share/i3blocks/volume 5 pulse
   #+END_SRC
*** Disk usage
   #+BEGIN_SRC sh :tangle ~/.config/i3/i3blocks.conf
   [disk]
   label=
   interval=30
   #+END_SRC
*** CPU usage
   #+BEGIN_SRC sh :tangle ~/.config/i3/i3blocks.conf
   [cpu_usage]
   label=
   interval=1
   min_width=CPU: 100.00%
   #+END_SRC
*** Date Time
   #+BEGIN_SRC sh :tangle ~/.config/i3/i3blocks.conf
   [time]
   command=date '+%b %d %H:%M'
   interval=5
   label=
   #+END_SRC
* Neovim
** General
*** Useful
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  if has("nvim")
  filetype plugin indent on
  syntax on
  set linebreak                      " break line without break the word
  set wrap
  set hidden                         " allow buffer swetching without saving

  set wildmenu                       " visual autocomplete for command menu
  set wildmode=list:longest,full     " Command <Tab> completion, list matches, then longest common part, then all
  set wildignorecase
  set wildchar=<Tab>
  #+end_src
*** Restore cursor position when opening file
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    augroup open
        autocmd!
        autocmd BufReadPost *
                    \ if line("'\"") > 1 && line("'\"") <= line("$") |
                    \   exe "normal! g`\"" |
                    \ endif
    augroup END
  #+end_src
*** Delete whiete spaces at save and transform tab into one space
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    augroup white
        autocmd!
        autocmd BufWritePre * :%s/\s\+$//e
        autocmd BufWritePre * silent! :%s/\t/ /g
    augroup END
  #+end_src
*** No backups and swapfile
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  set nobackup
  set noswapfile
  #+end_src
*** Horizontal windows always split below
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  set splitbelow                     "
  #+end_src
*** Vertical windows always split righ
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  set splitright
  #+end_src
*** Turn tabs into spaces
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  set expandtab
  set smarttab
  set smartindent
  #+end_src
*** show problematic spaces
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  set list
  set listchars=
  " set listchars+=tab:→\
  set listchars+=tab:▸→
  " set listchars+=tab:¤›
  set listchars+=trail:•
  set listchars+=extends:»
  set listchars+=precedes:«
  " set listchars+=eol:¬
  " set listchars+=nbsp:⣿
  " set fillchars=vert:│,fold:─
  #+end_src
*** Change working directory automatically
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    autocmd BufEnter,BufRead * silent! :cd%:p:h
  #+end_src
*** Scrolling
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  set scrolloff=8         "Start scrolling when we're 8 lines away from margins
  set sidescrolloff=15
  set sidescroll=1
  #+end_src
*** Search
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  set ignorecase        "case insensitive search
  set hlsearch          "highlight search
  set smartcase         "case sensitive if we type an uppercase
  set incsearch         "show match as search proceeds
  #+end_src
*** Autosave buffers before leaving them
   #+begin_src vimrc :tangle ~/.config/nvim/init.vim
   autocmd BufLeave * silent! :wa
   #+end_src
*** Spell for md
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    autocmd! BufRead,BufNewFile *.md setlocal spell spelllang=en_us
  #+end_src
*** Desabling neovim providers
   Nvim delegates some features to dynamic "providers".
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  " To disable Ruby support:
  let g:loaded_ruby_provider = 1
  " To disable Python 2 support:
  let g:loaded_python_provider = 1
  #+end_src
*** Enter automatically into the files directory
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  autocmd BufEnter * silent! lcd %:p:h
  #+end_src
** Plugins
*** Start Plug
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
	call plug#begin('~/.local/share/nvim/plugged')
    #+end_src
*** Coc
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    Plug 'neoclide/coc.nvim', {'tag': '*', 'branch': 'release'}
    #+end_src
*** Dart-vim-plugin
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
	Plug 'dart-lang/dart-vim-plugin'
    #+end_src
*** Denite
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
	Plug 'Shougo/denite.nvim'
    #+end_src
*** NeoSolarized
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  Plug 'iCyMind/NeoSolarized'
  #+end_src
*** Mustache
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  Plug 'mustache/vim-mustache-handlebars'
  #+end_src
*** Vim-go
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    Plug 'fatih/vim-go', { 'do': ':GoUpdateBinaries' }
  #+end_src
*** Vifm
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    Plug 'vifm/vifm.vim'
    #+end_src
*** Vim-easymotion
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
      Plug 'easymotion/vim-easymotion'
    #+end_src
*** Vista
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
      Plug 'liuchengxu/vista.vim'
    #+end_src
*** Ultisnips
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    Plug 'SirVer/ultisnips'
    #+end_src
*** vim-commentary
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    Plug 'tpope/vim-commentary'
    #+end_src
*** End Plug
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
	call plug#end()
    #+end_src
** Plugin Configuration
*** Coc
**** coc-setting.json
    #+begin_src json tangle ~/.config/nvim/coc-settings.json
      {
        "coc.preferences.formatOnSaveFiletypes": ["javascript"],
        "explorer.keyMappings": {
          "k": "nodePrev",
          "j": "nodeNext",

          "*": "toggleSelection",
          "<tab>": "actionMenu",

          "h": "collapse",
          "l": "expand",
          "J": ["toggleSelection", "normal:j"],
          "K": ["toggleSelection", "normal:k"],
          "gl": "expandRecursive",
          "gh": "collapseRecursive",
          "o": "expandOrCollapse",
          "<cr>": "open",
          "e": "open",
          "E": "openInVsplit",
          "t": "openInTab",
          "<bs>": "gotoParent",

          "y": "copyFilepath",
          "Y": "copyFilename",
          "c": "copyFile",
          "x": "cutFile",
          "p": "pasteFile",
          "d": "delete",
          "D": "deleteForever",

          "a": "addFile",
          "A": "addDirectory",
          "r": "rename",

          ".": "toggleHidden",
          "R": "refresh",

          "?": "help",
          "q": "quit",
          "X": "systemExecute",
          "gd": "listDrive",

          "f": "search",
          "F": "searchRecursive",

          "gf": "gotoSource:file",
          "gb": "gotoSource:buffer",

          "[[": "sourcePrev",
          "]]": "sourceNext",

          "[d": "diagnosticPrev",
          "]d": "diagnosticNext",

          "[c": "gitPrev",
          "]c": "gitNext",
          "<<": "gitStage",
          ">>": "gitUnstage"
        }
      }
    #+end_src
**** Get correct comment highligh in json files
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    autocmd FileType json syntax match Comment +\/\/.\+$+
    #+end_src
**** General configuration
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
      " Better display for messages
      set cmdheight=2

      " Smaller updatetime for CursorHold & CursorHoldI
      set updatetime=300

      " don't give |ins-completion-menu| messages.
      set shortmess+=c

      let g:coc_global_extensions = ['coc-ultisnips',
				   \ 'coc-go',
				   \ 'coc-flutter',
				   \ 'coc-conjure',
				   \ 'coc-prettier',
				   \ 'coc-css',
				   \ 'coc-eslint',
				   \ 'coc-json',
				   \ 'coc-tsserver',
				   \ 'coc-explorer',
				   \ 'coc-html',
				   \ 'coc-css',
				   \ 'coc-yaml',
				   \ 'coc-python',
				   \ 'coc-highlight',
				   \ 'coc-snippets',
				   \ 'coc-lists',
				   \ 'coc-angular',
				   \ 'coc-vimlsp',
				   \ 'coc-pairs']

      inoremap <silent><expr> <TAB>
	    \ pumvisible() ? coc#_select_confirm() :
	    \ coc#expandableOrJumpable() ? "\<C-r>=coc#rpc#request('doKeymap', ['snippets-expand-jump',''])\<CR>" :
	    \ <SID>check_back_space() ? "\<TAB>" :
	    \ coc#refresh()

      function! s:check_back_space() abort
	let col = col('.') - 1
	return !col || getline('.')[col - 1]  =~# '\s'
      endfunction

      let g:coc_snippet_next = '<tab>'

      " Use tab for trigger completion with characters ahead and navigate.
      " Use command ':verbose imap <tab>' to make sure tab is not mapped by other plugin.
      inoremap <silent><expr> <TAB>
	    \ pumvisible() ? "\<C-n>" :
	    \ <SID>check_back_space() ? "\<TAB>" :
	    \ coc#refresh()
      inoremap <expr><S-TAB> pumvisible() ? "\<C-p>" : "\<C-h>"

      function! s:check_back_space() abort
	let col = col('.') - 1
	return !col || getline('.')[col - 1]  =~# '\s'
      endfunction

      " Use <c-space> to trigger completion.
      inoremap <silent><expr> <c-space> coc#refresh()

      " Use <cr> to confirm completion, `<C-g>u` means break undo chain at current position.
      " Coc only does snippet and additional edit on confirm.
      inoremap <expr> <cr> pumvisible() ? "\<C-y>" : "\<C-g>u\<CR>"

      " Use `[c` and `]c` to navigate diagnostics
      nmap <silent> [c <Plug>(coc-diagnostic-prev)
      nmap <silent> ]c <Plug>(coc-diagnostic-next)

      " Remap keys for gotos
      nmap <silent> gd <Plug>(coc-definition)
      nmap <silent> gy <Plug>(coc-type-definition)
      nmap <silent> gi <Plug>(coc-implementation)
      nmap <silent> gr <Plug>(coc-references)
      nmap ge :CocCommand explorer<CR>

      " Use K to show documentation in preview window
      nnoremap <silent> K :call <SID>show_documentation()<CR>

      function! s:show_documentation()
	if (index(['vim','help'], &filetype) >= 0)
	  execute 'h '.expand('<cword>')
	else
	  call CocAction('doHover')
	endif
      endfunction

      " Highlight symbol under cursor on CursorHold
      " autocmd CursorHold * silent call CocActionAsync('highlight')

      " Remap for rename current word
      nmap <leader>rn <Plug>(coc-rename)

      " Remap for format selected region
      xmap <leader>f  <Plug>(coc-format-selected)
      nmap <leader>f  <Plug>(coc-format-selected)

      augroup mygroup
	autocmd!
	" Setup formatexpr specified filetype(s).
	autocmd FileType typescript,json setl formatexpr=CocAction('formatSelected')
	" Update signature help on jump placeholder
	autocmd User CocJumpPlaceholder call CocActionAsync('showSignatureHelp')
      augroup end

      " Remap for do codeAction of selected region, ex: `<leader>aap` for current paragraph
      xmap <leader>a  <Plug>(coc-codeaction-selected)
      nmap <leader>a  <Plug>(coc-codeaction-selected)

      " Remap for do codeAction of current line
      nmap <leader>ac  <Plug>(coc-codeaction)
      " Fix autofix problem of current line
      nmap <leader>qf  <Plug>(coc-fix-current)

      " Use `:Format` to format current buffer
      command! -nargs=0 Format :call CocAction('format')

      " Use `:Fold` to fold current buffer
      command! -nargs=? Fold :call     CocAction('fold', <f-args>)
    #+end_src
*** Dart-vim-plugin
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
      let dart_format_on_save = 1
    #+end_src
*** Denite
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
	call denite#custom#var('file/rec', 'command',
	\ ['ag', '--follow', '--nocolor', '--nogroup', '-g', ''])

      noremap <C-p> :DeniteProjectDir file/rec<CR>
      noremap <C-b> :Denite buffer<CR>

      autocmd FileType denite call s:denite_my_settings()
      function! s:denite_my_settings() abort
	nnoremap <silent><buffer><expr> <CR>
	\ denite#do_map('do_action')
	nnoremap <silent><buffer><expr> d
	\ denite#do_map('do_action', 'delete')
	nnoremap <silent><buffer><expr> p
	\ denite#do_map('do_action', 'preview')
	nnoremap <silent><buffer><expr> q
	\ denite#do_map('quit')
	nnoremap <silent><buffer><expr> i
	\ denite#do_map('open_filter_buffer')
	nnoremap <silent><buffer><expr> <Space>
	\ denite#do_map('toggle_select').'j'
      endfunction
    #+end_src
*** Ultisnips
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    let g:UltiSnipsExpandTrigger="<c-u>"
    let g:UltiSnipsJumpForwardTrigger="<c-l>"
    let g:UltiSnipsJumpBackwardTrigger="<c-h>"
    let g:UltiSnipsSnippetDirectories = ['/home/last/Projects/dots/snips/UltiSnips']
    " If you want :UltiSnipsEdit to split your window.
    let g:UltiSnipsEditSplit="vertical"
    #+end_src
*** Vim-go
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim

    let g:go_fmt_command = "goimports"
    " it is set by default now
    " let g:go_info_mode = "gocode"
    " let g:go_def_mode = "gopls"
    let g:go_autodetect_gopath = 1
    " Linters Runner for Go. 5x faster than gometalinter
    let g:go_metalinter_command='golangci-lint'
    let g:go_metalinter_autosave_enabled = ['vet', 'golint']
    let g:go_metalinter_enabled = ['vet', 'golint']


    " turn highlighting on
    let g:go_highlight_types = 1
    let g:go_highlight_fields = 1
    let g:go_highlight_functions = 1
    let g:go_highlight_methods = 1
    let g:go_highlight_structs = 1
    let g:go_highlight_operators = 1
    let g:go_highlight_build_constraints = 1
    let g:go_highlight_function_parameters = 1

    let g:go_gocode_propose_source = 1

    " enabled highlighting of variables that are the same
    " let g:go_auto_sameids = 1

    " Show type information and function signature
    let g:go_auto_type_info = 1

    " augroup auto_go
    " 	autocmd!
    " 	autocmd BufWritePost *.go :GoBuild
    " augroup end

    " Hide tabs
    autocmd BufNewFile,BufRead *.go setlocal invlist noexpandtab tabstop=4 shiftwidth=4
    autocmd BufReadPost *.gohtml set syntax=gotexttmpl
    #+end_src
*** Vim-easymotion
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
      " s{char}{char} to move to {char}{char}
      nmap s <Plug>(easymotion-overwin-f2)
    #+end_src
*** Vista
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim

    nnoremap <F3> :Vista!!<CR>
       " Set the executive for some filetypes explicitly. Use the explicit executive
      " instead of the default one for these filetypes when using `:Vista` without
      " specifying the executive.
      let g:vista_executive_for = {
	\ 'dart': 'coc',
	\ 'go': 'coc',
	\ }


      " Ensure you have installed some decent font to show these pretty symbols, then you can enable icon for the kind.
      let g:vista#renderer#enable_icon = 0

      " The default icons can't be suitable for all the filetypes, you can extend it as you wish.

      let g:vista#renderer#icons = {
      \   "function": "\ue929",
      \   "variable": "\uf71b",
      \  }
    #+end_src
** GUI
*** Theme
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    set termguicolors
    set background=dark
    colorscheme NeoSolarized
  #+end_src
*** No intro message
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  set shortmess=I
  #+end_src
*** Sign Column
*** Change sign column color
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
   highlight clear SignColumn
  #+end_src
*** Keep the sign colum always visible
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
   set signcolumn=yes
   autocmd! TermOpen * setlocal signcolumn=no
  #+end_src
*** Hide sigh column when in terminal
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
   autocmd! TermOpen * setlocal signcolumn=no
  #+end_src
*** Status Line
   #+begin_src vimrc
    set laststatus=0
   #+end_src
** Mapping
*** Leader
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    let mapleader = ","
    #+end_src
*** jk back to normal mode
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    inoremap jk <esc>
    tnoremap jk <C-\><C-n>
    #+end_src
*** Save file
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    nnoremap <leader>w :write<CR>
    #+end_src
*** Maximizied buffer to entire window
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    nnoremap <leader>m <C-W>o
    #+end_src
*** Remap ; -> :
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    nnoremap ; :
    tnoremap ; :
    #+end_src
*** Cicle between windows
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    nnoremap <space> <c-w>w
    #+end_src
*** File Navegation
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    " nnoremap <leader>a :argadd <c-r>=fnameescape(expand('%:p:h'))<cr>/*<C-d>
    " nnoremap <leader>b :b <C-d>
    " nnoremap <leader>d :e <c-r>=fnameescape(expand('%:p:h'))<cr>/<C-d>
    nnoremap <leader>q :b#<cr>
    #+end_src
*** Remap * -> *N
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    nnoremap * *N
    #+end_src
*** copy and paste from the clipboard system
    sudo apt install xclip
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    set clipboard+=unnamedplus
    nnoremap <leader>p "+p
    nnoremap  <leader>y "+y
    #+end_src
*** Tangle Org Files
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    function! Tangle()
    write
    silent! !emacs % --batch -f org-babel-tangle --kill
    redraw!
    redraws! | echohl Error | echon "Tangle: Done"| echohl None
    endfunc

    augroup org
        autocmd!
        autocmd BufRead,BufNewFile *.org set filetype=org
        autocmd FileType org nnoremap <f8> :call Tangle()<cr>
    augroup END
    #+end_src
*** Resizing
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
      nnoremap <Left> :vertical resize +2<CR>
      nnoremap <Right> :vertical resize -2<CR>
      nnoremap <Up> :resize -2<CR>
      nnoremap <Down> :resize +2<CR>
    #+end_src
*** Show next math string at the center of the screen
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    nnoremap n nzz
    nnoremap N Nzz
    #+end_src
*** Move opening curly braces to a new line
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
      inoremap <expr> <cr> getline(".")[col(".")-2:col(".")-1]=="{}" ? "<cr><esc>O" : "<cr>"
    #+end_src
*** Split line without quiting normal mode
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    nnoremap S :keeppatterns substitute/\s*\%#\s*/\r/e <bar> normal! ==<CR>
    #+end_src
** Abbreviations
   #+begin_src vimrc :tangle ~/.config/nvim/init.vim
   autocmd FileType go iabbrev ane !=
   autocmd FileType go iabbrev asv :=
   #+end_src
** Programming Languages
*** Type Script
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  autocmd! BufRead,BufNewFile *.ts set tabstop=2|set shiftwidth=2|set softtabstop=2|set expandtab|set smarttab|set smartindent
  #+end_src
*** Cpp
**** Run cpp source code
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    augroup cpp
        autocmd!
        autocmd FileType cpp nnoremap <F5> :call MonkeyTerminalExec('clang++ -std=c++1z ' . expand('%') . ' -o ' . expand('%<') . ' && ' . './' . expand('%<'))<cr>
        autocmd FileType cpp nnoremap <F6> :call CppTest()<cr>
    augroup END
    #+end_src
**** Indent
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  autocmd! FileType cpp set tabstop=4|set shiftwidth=4|set softtabstop=4|set expandtab|set smarttab|set smartindent
  #+end_src
*** javascript
**** Run javascript source code
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    augroup js
        autocmd!
        autocmd BufRead,BufNewFile *.js set filetype=javascript
        autocmd FileType javascript nnoremap <F5> :call MonkeyTerminalExec('node ' . expand('%'))<cr>
    augroup END
    #+end_src
**** Indent
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  autocmd! FileType javascript set tabstop=2|set shiftwidth=2|set softtabstop=2|set expandtab|set smarttab|set smartindent
  #+end_src
**** Format js file at saving
     #+begin_src vimrc :tangle ~/.config/nvim/init.vim
       command! -nargs=0 Prettier :CocCommand prettier.formatFile
       augroup formatJs
	  autocmd!
	  autocmd BufWritePre *.js :Prettier
       augroup END
     #+end_src
*** python
**** Python3 provider path
   Nvim delegates some features to dynamic "providers".
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    let g:python3_host_prog = '/home/last/bin/anaconda3/bin/python3'
    #+end_src
**** Run python source code
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    augroup python
        autocmd!
        autocmd FileType python nnoremap <F5> :call MonkeyTerminalExec('python ' . expand('%'))<cr>
    augroup END
    #+end_src
**** Indent
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  autocmd! FileType python set tabstop=4|set shiftwidth=4|set softtabstop=4|set expandtab|set smarttab|set smartindent
  #+end_src
*** go
**** Run go code
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    augroup go
        autocmd!
        autocmd BufRead,BufNewFile *.go set filetype=go
        autocmd FileType go nnoremap <F5> :call MonkeyTerminalExec('go run ' . expand('%'))<cr>
    augroup END
    #+end_src
*** dart
**** Run Dart code
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    augroup dart
        autocmd!
        autocmd BufRead,BufNewFile *.dart set filetype=dart
        autocmd FileType dart nnoremap <F5> :call MonkeyTerminalExec('dart ' . expand('%'))<cr>
    augroup END
    #+end_src
**** Format dart file at saving
  #+begin_src vimrc
    augroup formatDart
        autocmd!
        autocmd BufWritePre *.dart :DartFmt
    augroup END
  #+end_src
**** Indent
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  autocmd! FileType dart set tabstop=2|set shiftwidth=2|set softtabstop=2|set expandtab|set smarttab|set smartindent
  #+end_src
*** rust
**** Run Rust code
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    augroup rust
        autocmd!
        autocmd BufRead,BufNewFile *.rs set filetype=rust
        autocmd FileType rust nnoremap <F5> :call MonkeyTerminalExec('rustc ' . expand('%') . ' && ' . './' . expand('%<'))<cr>
    augroup END
    #+end_src
*** java
**** Run Java code
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
        augroup java
        autocmd!
        autocmd BufRead,BufNewFile *.java set filetype=java
        autocmd FileType java nnoremap <F5> :call MonkeyTerminalExec('javac ' . expand('%') . ' && ' . 'java ' . expand('%<'))<cr>
    augroup END
    #+end_src
*** kotlin
**** Run Kotlin code
    #+begin_src vimrc :tangle ~/.config/nvim/init.vim
        augroup kotlin
        autocmd!
        autocmd BufRead,BufNewFile *.kt set filetype=kotlin
        autocmd FileType kotlin nnoremap <F5> :call MonkeyTerminalExec('kotlinc ' . expand('%') . ' -include-runtime -d ' . expand('%<') . '.jar' . ' && ' . 'java -jar ' . expand('%<') . '.jar')<cr>
    augroup END
    #+end_src
*** html
**** Indent
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
  autocmd! FileType html set tabstop=4|set shiftwidth=4|set softtabstop=4|set expandtab|set smarttab|set smartindent
  #+end_src
** Functions
*** Monkey Terminal
  #+begin_src vimrc :tangle ~/.config/nvim/init.vim
    " The default value when a window is not found is -1
    let s:monkey_terminal_window = -1
    let s:monkey_terminal_buffer = -1
    let s:monkey_terminal_job_id = -1
    function! MonkeyTerminalOpen()
      " Check if buffer exists, if not create a window and a buffer
      if !bufexists(s:monkey_terminal_buffer)
        " Creates a window call monkey_terminal
        new monkey_terminal
        " Moves to the window below the current one
        wincmd L
        " resize the high of the window
        " exec 'resize 7'

         let s:monkey_terminal_job_id = termopen($SHELL, { 'detach': 1 })

         " Change the name of the buffer to "Terminal 1"
         silent file Terminal\ 1
         " Gets the id of the terminal window
         let s:monkey_terminal_window = win_getid()
         let s:monkey_terminal_buffer = bufnr('%')

        " The buffer of the terminal won't appear in the list of the buffers
        " when calling :buffers command
        set nobuflisted
      else
        if !win_gotoid(s:monkey_terminal_window)
        sp
        " Moves to the window below the current one
        wincmd L
        " resize the high of the window
        " exec 'resize 7'
        buffer Terminal\ 1
         " Gets the id of the terminal window
         let s:monkey_terminal_window = win_getid()
        endif
      endif
    endfunction

    function! MonkeyTerminalToggle()
      if win_gotoid(s:monkey_terminal_window)
        call MonkeyTerminalClose()
      else
        call MonkeyTerminalOpen()
      endif
    endfunction

    function! MonkeyTerminalClose()
      if win_gotoid(s:monkey_terminal_window)
        " close the current window
        hide
      endif
    endfunction

    function! MonkeyTerminalExec(cmd)
      if !win_gotoid(s:monkey_terminal_window)
        call MonkeyTerminalOpen()
      endif

      " clear current input
      call jobsend(s:monkey_terminal_job_id, "clear\n")

      " run cmd
      call jobsend(s:monkey_terminal_job_id, a:cmd . "\n")
      normal! G
      wincmd p
    endfunction

    nnoremap <F7> :call MonkeyTerminalToggle()<cr>
    tnoremap <F7> <C-\><C-n>:call MonkeyTerminalToggle()<cr>
    endif
  #+end_src
*** Open and close Terminal with F9
  #+begin_src vimrc
  let g:term_buf = 0
  let g:term_win = 0
    function! Term_toggle(height)
        if win_gotoid(g:term_win)
            hide
        else
            botright new
            exec "resize " . a:height
            try
                exec "buffer " . g:term_buf
            catch
                call termopen("$SHELL", {"detach": 0})
                let g:term_buf = bufnr("")
            endtry
            startinsert!
            let g:term_win = win_getid()
        endif
    endfunction

    nnoremap <F9> :call Term_toggle(10)<cr>
    tnoremap <F9> <C-\><C-n>:call Term_toggle(10)<cr>
  #+end_src
* Vifm
** Theme
   #+BEGIN_SRC sh :tangle ~/.vifm/colors/solarized-dark.vifm
     " VIFM COLORS
     " solarized dark
     " based on: https://github.com/istib/dotfiles/blob/master/vifm/vifm-colors

     " Default = -1 = None, can be used for transparency or default color
     " Black = 0
     " Red = 1
     " Green = 2
     " Yellow = 3
     " Blue = 4
     " Magenta = 5
     " Cyan = 6
     " White = 7

     " STYLES
     " bold
     " underline
     " reverse or inverse
     " standout
     " none


     highlight clear

     highlight Win             cterm=none ctermfg=default    ctermbg=none

     highlight TopLine         cterm=none ctermfg=blue       ctermbg=none
     highlight TopLineSel      cterm=none ctermfg=blue       ctermbg=none
     highlight StatusLine      cterm=none ctermfg=blue       ctermbg=none
     highlight Border          cterm=none ctermfg=blue       ctermbg=none

     highlight Selected        cterm=bold ctermfg=magenta    ctermbg=default
     highlight CurrLine        cterm=bold ctermfg=default    ctermbg=blue

     highlight WildMenu        cterm=underline,reverse ctermfg=white ctermbg=black
     highlight CmdLine         cterm=none ctermfg=white ctermbg=black
     highlight ErrorMsg        cterm=none ctermfg=red ctermbg=black

     highlight Directory       cterm=none ctermfg=cyan ctermbg=default
     highlight Link            cterm=none ctermfg=yellow ctermbg=default
     highlight BrokenLink      cterm=none ctermfg=red ctermbg=default
     highlight Socket          cterm=none ctermfg=magenta ctermbg=default
     highlight Device          cterm=none ctermfg=red ctermbg=default
     highlight Fifo            cterm=none ctermfg=cyan ctermbg=default
     highlight Executable      cterm=none ctermfg=green ctermbg=default
   #+END_SRC
** Configuration
   #+BEGIN_SRC sh :tangle ~/.vifm/vifmrc
     " This is the actual command used to start vi.  The default is vim.
     " If you would like to use another vi clone such Elvis or Vile
     " you will need to change this setting.

     set vicmd=nvim
     " set vicmd=elvis\ -G\ termcap
     " set vicmd=vile

     " Trash Directory
     " The default is to move files that are deleted with dd or :d to
     " the trash directory.  If you change this you will not be able to move
     " files by deleting them and then using p to put the file in the new location.
     " I recommend not changing this until you are familiar with vifm.
     " This probably shouldn't be an option.

     set trash

     " This is how many directories to store in the directory history.

     set history=100

     " Automatically resolve symbolic links on l or Enter.

     set nofollowlinks

     " With this option turned on you can run partially entered commands with
     " unambiguous beginning using :! (e.g. :!Te instead of :!Terminal or :!Te<tab>).

     set fastrun

     " Natural sort of (version) numbers within text.

     set sortnumbers

     " Maximum number of changes that can be undone.

     set undolevels=100

     " If you installed the vim.txt help file set vimhelp.
     " If would rather use a plain text help file set novimhelp.

     set novimhelp

     " If you would like to run an executable file when you
     " press return on the file name set this.

     set norunexec

     " Use KiB, MiB, ... instead of KB, MB, ...

     set noiec

     " Selected color scheme

     colorscheme solarized-dark.vifm

     " The FUSE_HOME directory will be used as a root dir for all FUSE mounts.
     " Unless it exists with write/exec permissions set, vifm will attempt to
     " create it.

     set fusehome=/tmp/vifm_FUSE

     " Format for displaying time in file list. For example:
     " TIME_STAMP_FORMAT=%m/%d-%H:%M
     " See man date or man strftime for details.

     set timefmt=%m/%d\ %H:%M

     " Show list of matches on tab complition in command-line mode

     set wildmenu
     " set wildstyle=popup
     " set suggestoptions=normal,visual,view,otherpane,keys,marks,registers

     " Ignore case in search patterns unless it contains at least one uppercase
     " letter

     set ignorecase
     set smartcase

     " Don't highlight search results automatically

     set nohlsearch

     " Use increment searching (search while typing)
     set incsearch

     " Try to leave some space from cursor to upper/lower border in lists

     set scrolloff=4

     " Don't do to much requests to slow file systems

     set slowfs=curlftpfs

     " Set custom status line look

     set statusline="  %t%= %A %10u:%-7g %15s %20d  "

     " ------------------------------------------------------------------------------

     " :mark mark /full/directory/path [filename]

     mark e ~/.emacs.d
     mark D ~/Documents
     mark d ~/Downloads
     mark p ~/Projects
     mark g ~/bin/go/src/myGo
     mark b ~/bin/
     mark h ~/

     " ------------------------------------------------------------------------------

     " :com[mand][!] command_name action
     " The following macros can be used in a command
     " %a is replaced with the user arguments.
     " %c the current file under the cursor.
     " %C the current file under the cursor in the other directory.
     " %f the current selected file, or files.
     " %F the current selected file, or files in the other directory.
     " %b same as %f %F.
     " %d the current directory name.
     " %D the other window directory name.
     " %m run the command in a menu window

     " Check this website to see the meaning of all macros
     " http://priede.bf.lu.lv/ftp/pub/Faili/Vifm/Vifm_manual-0.8.2.html#mozTocId681414

     command! df df -h %m 2> /dev/null
     command! diff vim -d %f %F
     command! zip zip -r %f.zip %f
     command! run !! ./%f
     command! make !!make %a
     command! mkcd :mkdir %a | cd %a
     command! vgrep vim "+grep %a"
     command! reload :write | restart
     " command! touch :touch %a

     " ------------------------------------------------------------------------------

     " The file type is for the default programs to be used with
     " a file extension.
     " :filetype pattern1,pattern2 defaultprogram,program2
     " :fileviewer pattern1,pattern2 consoleviewer
     " The other programs for the file type can be accessed with the :file command
     " The command macros %f, %F, %d, %F may be used in the commands.
     " The %a macro is ignored.  To use a % you must put %%.

     " For automated FUSE mounts, you must register an extension with :file[x]type
     " in one of following formats:
     "
     " :filetype extensions FUSE_MOUNT|some_mount_command using %SOURCE_FILE and %DESTINATION_DIR variables
     " %SOURCE_FILE and %DESTINATION_DIR are filled in by vifm at runtime.
     " A sample line might look like this:
     " :filetype *.zip,*.jar,*.war,*.ear FUSE_MOUNT|fuse-zip %SOURCE_FILE %DESTINATION_DIR
     "
     " :filetype extensions FUSE_MOUNT2|some_mount_command using %PARAM and %DESTINATION_DIR variables
     " %PARAM and %DESTINATION_DIR are filled in by vifm at runtime.
     " A sample line might look like this:
     " :filetype *.ssh FUSE_MOUNT2|sshfs %PARAM %DESTINATION_DIR
     " %PARAM value is filled from the first line of file (whole line).
     " Example first line for SshMount filetype: root@127.0.0.1:/
     "
     " You can also add %CLEAR if you want to clear screen before running FUSE
     " program.

     " Pdf
     filextype *.pdf okular %c %i &, zathura %c %i &, apvlv %c, xpdf %c
     fileviewer *.pdf pdftotext -nopgbrk %c -

     " Torrent
     filextype *.torrent transmission-gtk %c %i &

     " PostScript
     filextype *.ps,*.eps,*.ps.gz
	     \ {View in zathura}
	     \ zathura %f,
	     \ {View in gv}
	     \ gv %c %i &,

     " Djvu
     filextype *.djvu
	     \ {View in zathura}
	     \ zathura %f,
	     \ {View in apvlv}
	     \ apvlv %f,

     " Audio
     filetype *.wav,*.mp3,*.flac,*.ogg,*.m4a,*.wma,*.ape,*.ac3
	    \ {Play using cmus}
	    \ cmus %c,
	    \ {Play using ffplay}
	    \ ffplay -nodisp %c,
	    \ {Play using MPlayer}
	    \ mplayer %f,
	    \ ffplay %c,
     fileviewer *.mp3 mp3info
     fileviewer *.flac soxi

     " Video
     filextype *.avi,*.mp4,*.wmv,*.dat,*.3gp,*.ogv,*.mkv,*.mpg,*.vob,*.flv,*.m2v,*.mov,*.webm,*.ts,*.m4v
	     \ {View using vlc}
	     \ cvlc %f & &>/dev/null,
	     \ {View using ffplay}
	     \ ffplay -fs %c,
	     \ {View using Dragon}
	     \ dragon %f,
	     \ {View using mplayer}
	     \ mplayer %f,
     fileviewer *.avi,*.mp4,*.wmv,*.dat,*.3gp,*.ogv,*.mkv,*.mpg,*.vob,*.flv,*.m2v,*.mov,*.webm,*.ts,*.m4v
	      \ ffprobe -pretty %c 2>&1

     " Web
     filextype *.html,*.htm
	     \ {Open with dwb}
	     \ dwb %f %i &,
	     \ {Open with firefox}
	     \ firefox %f &,
	     \ {Open with uzbl}
	     \ uzbl-browser %f %i &,
     filetype *.html,*.htm links, lynx

     " Object
     filetype *.o nm %f | less

     " Man page
     filetype *.[1-8] gtbl %c | groff -Tascii -man | less
     fileviewer *.[1-8] gtbl %c | groff -Tascii -man | col -b

     " Image
     filextype *.bmp,*.jpg,*.jpeg,*.png,*.gif,*.xpm
	     \ {View in sxiv}
	     \ sxiv,
	     \ {View in gpicview}
	     \ gpicview %c,
     fileviewer *.bmp,*.jpg,*.jpeg,*.png,*.gif,*.xpm convert -identify %f -verbose /dev/null

     " MD5
     filetype *.md5
	    \ {Check MD5 hash sum}
	    \ md5sum -c %f,

     " GPG signature
     filetype *.asc
	    \ {Check signature}
	    \ !!gpg --verify %c,

     " Torrent
     filetype *.torrent ktorrent %f &
     fileviewer *.torrent dumptorrent -v %c

     " FuseZipMount
     filetype *.zip,*.jar,*.war,*.ear,*.oxt
            \ {Opent with file-roller}
	    \ file-roller %c &
     fileviewer *.zip,*.jar,*.war,*.ear,*.oxt zip -sf %c

     " ArchiveMount
     filetype *.tar,*.tar.bz2,*.tbz2,*.tgz,*.tar.gz,*.tar.xz,*.txz
	    \ {Mount with archivemount}
	    \ FUSE_MOUNT|archivemount %SOURCE_FILE %DESTINATION_DIR,
            \ {Opent with file-roller}
	    \ file-roller %c &
     fileviewer *.tgz,*.tar.gz tar -tzf %c
     fileviewer *.tar.bz2,*.tbz2 tar -tjf %c
     fileviewer *.tar.txz,*.txz xz --list %c

     " Rar2FsMount and rar archives
     filetype *.rar
	    \ {Mount with rar2fs}
	    \ FUSE_MOUNT|rar2fs %SOURCE_FILE %DESTINATION_DIR,
            \ {Opent with file-roller}
	    \ file-roller %c &
     fileviewer *.rar unrar v %c

     " IsoMount
     filetype *.iso
	    \ {Mount with fuseiso}
	    \ FUSE_MOUNT|fuseiso %SOURCE_FILE %DESTINATION_DIR,

     " SshMount
     filetype *.ssh
	    \ {Mount with sshfs}
	    \ FUSE_MOUNT2|sshfs %PARAM %DESTINATION_DIR,

     " FtpMount
     filetype *.ftp
	    \ {Mount with curlftpfs}
	    \ FUSE_MOUNT2|curlftpfs -o ftp_port=-,,disable_eprt %PARAM %DESTINATION_DIR,

     " Fuse7z and 7z archives
     filetype *.7z
	    \ {Mount with fuse-7z}
	    \ FUSE_MOUNT|fuse-7z %SOURCE_FILE %DESTINATION_DIR,
     fileviewer *.7z 7z l %c

     " Office files
     filextype *.odt,*.doc,*.docx,*.xls,*.xlsx,*.odp,*.pptx libreoffice %f &
     fileviewer *.doc catdoc %c
     fileviewer *.docx, docx2txt.pl %f -

     " TuDu files
     filetype *.tudu tudu -f %c

     " Qt projects
     filextype *.pro qtcreator %f &

     " Directories
     filextype */
	     \ {View in thunar}
	     \ Thunar %f &,
     fileviewer .*/,*/ tree %f

     " Syntax highlighting in preview
     "
     " Explicitly set highlight type for some extensions
     "
     " 256-color terminal
     " fileviewer *.[ch],*.[ch]pp highlight -O xterm256 -s dante --syntax c %c
     " fileviewer Makefile,Makefile.* highlight -O xterm256 -s dante --syntax make %c
     "
     " 16-color terminal
     " fileviewer *.c,*.h highlight -O ansi -s dante %c
     "
     " Or leave it for automatic detection
     "
     " fileviewer * pygmentize -O style=monokai -f console256 -g

     " Displaying pictures in terminal
     "
     " fileviewer *.jpg,*.png shellpic %c

     " Open all other files with default system programs (you can also remove all
     " :file[x]type commands above to ensure they don't interfere with system-wide
     " settings).  By default all unknown files are opened with 'vi[x]cmd'
     " uncommenting one of lines below will result in ignoring 'vi[x]cmd' option
     " for unknown file types.
     " For *nix:
     " filetype * xdg-open
     " For OS X:
     " filetype * open
     " For Windows:
     " filetype * start, explorer

     " ------------------------------------------------------------------------------

     " What should be saved automatically between vifm runs
     " Like in previous versions of vifm
     " set vifminfo=options,filetypes,commands,bookmarks,dhistory,state,cs
     " Like in vi
     set vifminfo=dhistory,savedirs,chistory,state,tui,shistory,
	 \phistory,fhistory,dirstack,registers,bookmarks

     " ------------------------------------------------------------------------------

     " Examples of configuring both panels

     " Customize view columns a bit (enable ellipsis for truncated file names)
     "
     " windo set viewcolumns=-{name}..,6{}.

     " Filter-out build and temporary files
     "
     " windo filter! /^.*\.(lo|o|d|class|py[co])$|.*~$/

     " ------------------------------------------------------------------------------

     " Sample mappings
     nnoremap qq :quit<cr>

     " Start shell in current directory
     nnoremap s :shell<cr>

     " Display sorting dialog
     nnoremap S :sort<cr>

     " Toggle visibility of preview window
     nnoremap w :view<cr>
     vnoremap w :view<cr>gv

     " Open file in existing instance of gvim
     nnoremap o :!gvim --remote-tab-silent %f<cr>
     " Open file in new instance of gvim
     " nnoremap O :!gvim %f<cr>

     " Open file in the background using its default program
     " nnoremap gb :file &<cr>l

     " Yank current directory path into the clipboard xclip or xsel
     nnoremap yd :!echo %d | xsel %i<cr>

     " Yank current file path into the clipboard
     nnoremap yf :!echo %c:p | xsel %i<cr>

     " Mappings for faster renaming
     nnoremap I cw<c-a>
     nnoremap cc cw<c-u>
     nnoremap A cw<c-w>

     " Open console in current directory
     nnoremap ,t :!tilix &<cr>

     " Open vim to edit vifmrc and apply settings after returning to vifm
     " nnoremap ,c :write | execute ':!vim $MYVIFMRC' | restart<cr>
     " Open gvim to edit vifmrc
     " nnoremap ,C :!gvim --remote-tab-silent $MYVIFMRC &<cr>

     " Toggle wrap setting on ,w key
     " nnoremap ,w :set wrap!<cr>

     nnoremap ; :

     " Example of standard two-panel file managers mappings
     nnoremap <f3> :!less %f<cr>
     nnoremap <f4> :edit<cr>
     nnoremap <f5> :copy<cr>
     nnoremap <f6> :move<cr>
     nnoremap <f7> :mkdir<space>
     nnoremap <f8> :delete<cr>

     " ------------------------------------------------------------------------------

     " Various customization examples

     " Use ag (the silver searcher) instead of grep
     "
     " set grepprg=ag\ --line-numbers\ %i\ %a\ %s

     " Add additional place to look for executables
     "
     " let $PATH=$HOME.'/bin/fuse:'.$PATH

     " Block particular shortcut
     "
     " nnoremap <left> <nop>
   #+END_SRC
* Zsh
** Theme
   #+BEGIN_SRC sh :tangle ~/.zshrc
     POWERLEVEL9K_MODE='nerdfont-complete'
     POWERLEVEL9K_PROMPT_ON_NEWLINE=true
     POWERLEVEL9K_DIR_OMIT_FIRST_CHARACTER=true
     POWERLEVEL9K_SHORTEN_DIR_LENGTH=1
     POWERLEVEL9K_LEFT_SEGMENT_SEPARATOR="\uE0B4"
     POWERLEVEL9K_RIGHT_SEGMENT_SEPARATOR="\uE0B6"
     POWERLEVEL9K_MULTILINE_FIRST_PROMPT_PREFIX=""
     POWERLEVEL9K_MULTILINE_LAST_PROMPT_PREFIX="\ue0b4 "
     POWERLEVEL9K_ROOT_ICON=$'\uF198'  # 
     POWERLEVEL9K_LEFT_PROMPT_ELEMENTS=(root_indicator dir vcs)
     POWERLEVEL9K_RIGHT_PROMPT_ELEMENTS=(status)
     ZSH_THEME="powerlevel9k/powerlevel9k"
     # ZSH_THEME="robbyrussell"
   #+END_SRC
** Default Editor
   #+BEGIN_SRC sh :tangle ~/.zshrc
     export EDITOR=/home/last/bin/emacs/lib-src/emacsclient
     export ALTERNATE_EDITOR=/home/last/bin/emacs/src/emacs
     #sudo -e
     export SUDO_EDITOR=/home/last/bin/nvim
   #+END_SRC
** Paths
*** Zsh
   #+BEGIN_SRC sh :tangle ~/.zshrc
   export ZSH=$HOME/.oh-my-zsh
   source $ZSH/oh-my-zsh.sh
   #+END_SRC
*** Env
   #+BEGIN_SRC sh :tangle ~/.zshrc
   export PATH="$HOME/bin:/snap/bin:/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin"
   #+END_SRC

*** VSCode
   #+BEGIN_SRC sh :tangle ~/.zshrc
     PATH=$HOME/bin/VSCode:$PATH
   #+END_SRC

*** Universal-ctags
   #+BEGIN_SRC sh :tangle ~/.zshrc
     PATH=$HOME/bin/universal-ctags/bin:$PATH
   #+END_SRC

*** Java
   #+BEGIN_SRC sh :tangle ~/.zshrc
   PATH=$HOME/bin/java/jdk-10.0.1/bin:$PATH
   #+END_SRC
*** Dart
   #+BEGIN_SRC sh :tangle ~/.zshrc
   export PATH=$HOME/.pub-cache/bin:$PATH
   export PATH=$HOME/bin/dart-sdk/bin:$PATH
   #+END_SRC

*** Go
   #+BEGIN_SRC sh :tangle ~/.zshrc
   GO111MODULES=on
   PATH=$PATH:/usr/local/go/bin
   export GOPATH=$HOME/bin/go
   PATH=$PATH:$HOME/bin/go/bin
   #+END_SRC

*** Clang
   #+BEGIN_SRC sh :tangle ~/.zshrc
   PATH=$HOME/bin/clang/bin:$PATH
   #+END_SRC

*** Node
   #+BEGIN_SRC sh :tangle ~/.zshrc
   PATH=$HOME/bin/node/bin:$PATH
   #+END_SRC

*** Python
   #+BEGIN_SRC sh :tangle ~/.zshrc
   PATH=$HOME/bin/anaconda3/bin:$PATH
   #+END_SRC

*** Flutter
   #+BEGIN_SRC sh :tangle ~/.zshrc
   PATH=$HOME/bin/flutter/bin:$PATH
   #+END_SRC
*** Flutter-dev
   #+BEGIN_SRC
   PATH=$HOME/bin/flutter-dev/bin:$PATH
   #+END_SRC
*** Cmake
   #+BEGIN_SRC sh :tangle ~/.zshrc
   PATH=$HOME/bin/cmake/bin:$PATH
   #+END_SRC
*** Flatbuffers
   #+BEGIN_SRC sh :tangle ~/.zshrc
   PATH=$HOME/bin/flatbuffers:$PATH
   #+END_SRC
*** Vifm
   #+BEGIN_SRC sh :tangle ~/.zshrc
   PATH=$HOME/bin/vifm/bin:$PATH
   #+END_SRC
*** Android-SDK
   #+BEGIN_SRC sh :tangle ~/.zshrc
   export ANDROID_HOME=$HOME/bin/android-sdk
   #+END_SRC
*** Emacs
   #+BEGIN_SRC sh :tangle ~/.zshrc
   PATH=$HOME/bin/emacs/src:$PATH
   PATH=$HOME/bin/emacs/lib-src:$PATH
   #+END_SRC
*** Dictionaries
    This path needs to be set so ispell work with hunspell

   #+BEGIN_SRC sh :tangle ~/.zshrc
     export DICPATH=/usr/share/hunspell
     export LANG=en_US.UTF-8
   #+END_SRC
** Plugins
   #+BEGIN_SRC sh :tangle ~/.zshrc
   plugins=(git wd colored-man-pages)
   #+END_SRC
** Alias
   #+BEGIN_SRC sh :tangle ~/.zshrc
   alias vf="vifm ."
   alias n="nvim"
   alias yd="youtube-dl --extract-audio --audio-format mp3 -o \"%(title)s.%(ext)s\""
   #+END_SRC
